# Transforms "var" package's forecast error variance decomposition to tidy
#
# To input the data as ggplots, the data has to be put in to tidy
# format first.
#

tidyFEVD_VAR <- function() {
  print("# I will transform FEVD to tidy format")
}
